[pytest]
minversion = 7.0
testpaths = autotests tests
addopts = -ra
markers =
    smoke: quick smoke tests for critical paths
    regression: broader regression checks
    slow: slow-running tests
    testcase: external test case ID (e.g. AZ-LG-001)